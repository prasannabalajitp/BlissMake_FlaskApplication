# MongoDB URI
# Replace <username>, <password>, <hostname>, <port>, and <database> with your MongoDB details.
# For example: mongodb://user:password@localhost:27017/blissmake
MONGO_URI=mongodb://localhost:27017/blissmake

# Secret key for session management and other cryptographic operations
# Ensure this is a random, secure string for production use
SECRET_KEY=your_secret_key

# UPI ID and payment details (if applicable)
# Replace with your actual UPI details if payment processing is configured
UPI_ID=your_upi_id
PAYEE_NAME=your_payee_name
